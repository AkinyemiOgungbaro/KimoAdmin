name: kimo_admin
description: KIMO GAMES Admin Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  file_picker: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
